/**
 * The root (parent) node MUST be the largest value recursively than its children
 *
 * @class MaxHeap
 */
class MaxHeap {
  add() {
    // Find the remaining place to insert
  }
  swap() {
    // Is swapping required - do it recursively
  }
  delete() {
    // 1. Remove the root node from the heap
    // 2. Replace the deleted node with the node furthest to the right (leaf node)
    // 3. Heapify -
    // - start with the root, start comparing with the children to swap
  }
}
